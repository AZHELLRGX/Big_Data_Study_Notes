package org.azhell.learn.scala.processcontrol

/**
 * 测试scala中的while循环语法
 */
object TestWhile {
  def main(args: Array[String]): Unit = {
    /*
     * scala中的while与do while循环语法与java的语法规则一模一样
     * 但是scala不推荐使用while循环：因为while循环的返回值是Unit
     */

  }
}
